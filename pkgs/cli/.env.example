# Network configuration
# Options: standalone, testnet-local, testnet
NETWORK_ENV_VAR=testnet

# Wallet seed (KEEP SECRET!)
# Generate a new seed for production use
SEED_ENV_VAR=your-wallet-seed-here

# Counter contract specific
INITIAL_COUNTER_ENV_VAR=0

# Cache file for wallet state
# Optional: If not set, a default name will be generated
CACHE_FILE_ENV_VAR=

# Contract address (for verification)
CONTRACT_ADDRESS=

# Patient Registry specific
# No additional environment variables required for Patient Registry
# The deployment script will use the same NETWORK_ENV_VAR, SEED_ENV_VAR, and CACHE_FILE_ENV_VAR

# Optional: Custom RPC endpoint
# RPC_URL=https://rpc.testnet-02.midnight.network/

# Optional: Custom Indexer endpoint
# INDEXER_URL=https://indexer.testnet-02.midnight.network/